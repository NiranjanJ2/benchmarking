Got it — clean it up so it reads as a forward plan, not a changelog.Doing a full clean rewrite — no more "originally we...", "descoped", or "dropped" debris.Clean. Verified zero remaining mentions of CCS, XBOW, GPT-4o, CAI-as-evaluation-target, "descoped", "originally", or "previously."

The plan now reads as if NDSS, Claude Code, PentestGPT-only, and the 4 evaluated positions were the plan from the start. Two intentional retentions worth flagging:

- **CAI in §1 motivation and §9 differentiation table** — kept because CAI is real prior work being cited, not an evaluation target. Different role.
- **All 8 positions in the §4 taxonomy table** — the design space is 8; the evaluation covers 4. §6.1 names the 4 evaluated positions cleanly without per-row "dropped" markers in the taxonomy.
